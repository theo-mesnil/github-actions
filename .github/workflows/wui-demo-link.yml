on:
  pull_request:
    types: [opened]
jobs:
  add_links_to_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print the url to the demo
        run: echo The url is https://${GITHUB_HEAD_REF}--welcome-ui.netlify.app
